<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Acción para abrir las vistas -->
    <record id="action_sale_order_category" model="ir.actions.act_window">
        <field name="name">Categorias de Venta</field>
        <field name="res_model">sale.order.category</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menú dentro de Configuración de Ventas -->
    <menuitem
            id="menu_sale_order_category"
            name="Categorías de Venta"
            parent="sale.menu_sale_config"
            action="action_sale_order_category"
            sequence="20"/>
</odoo>
